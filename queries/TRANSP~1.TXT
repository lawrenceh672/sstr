SELECT 	COUNT(TRANSCD) AS R_TOT,
	(SELECT COUNT(TRANSCD) FROM FACCSDB.TRPTDATA WHERE TRANSCD = 'A') AS A_TOT,
	(SELECT COUNT(TRANSCD) FROM FACCSDB.TRPTDATA WHERE TRANSCD = 'G') AS G_TOT
FROM 	FACCSDB.TRPTDATA WHERE TRANSCD = 'R'
